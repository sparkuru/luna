# 跨平台离线优先记账软件 PRD

## 0. 文档信息

- 文档状态：已确认的产品基线
- 文档版本：0.2
- 更新时间：2026-08-30
- 产品暂名：TBD
- 产品类型：开源、个人业余项目
- 目标用户：个人、伴侣和小型家庭

本文档描述产品要解决的问题、明确承诺的行为和范围。具体密码算法及参数、密文对象布局、因果同步协议、对象存储兼容性矩阵、密钥轮换方案和浏览器存储验证结果，均不在本文档中提前定案，见“待研究与延期事项”。

## 1. 产品概述

本产品是一个跨平台、离线优先的个人收入/支出记录器。用户可以在没有网络时记录收入和支出、查看统计、设置可选的月度开支上限并添加附件；网络恢复后，客户端将加密数据同步到用户自己提供的 S3/OSS 兼容对象存储，并从中拉取其他设备的变更。

产品的本质是“收入/支出记录器 + 可选月度预算”，而不是银行账户、资产负债或专业会计系统。产品不维护现金、银行卡、钱包等账户的实际余额。用户可以把现金、银行卡、电子钱包等作为支付方式标签，帮助检索和统计，但它们不产生账户余额或账户间转账语义。

客户端解锁后，账目在端侧以明文可见；对象存储只保存客户端加密后的密文。新设备只需用户提供对象存储地址/配置、有效存储凭据和本地主密码即可恢复，不要求旧设备在线，也不依赖专用同步服务器。

核心原则：

1. 离线记账是完整能力，网络不是保存收入或支出的前提。
2. 本地保存先于同步；对象存储不可用时不阻塞录入、查询和本地统计。
3. 服务端或对象存储只接触密文及完成对象操作所必需的最少元数据。
4. 财务关键字段的并发修改不能被最后写入规则静默覆盖。
5. 修改和删除可追溯，统计只使用最新有效版本。
6. 数据可以恢复、备份和导出，用户不被某个服务商或本地数据库格式锁定。
7. 日常录入保持简单，只有收入、支出两种交易类型。

## 2. 用户问题

现有记账工具常见的问题包括：

- 没有网络时无法可靠记账，或所谓离线能力只是短期缓存。
- 数据集中存放在第三方云端，用户难以控制隐私、可用性和迁移。
- 多设备同时修改时采用最后写入覆盖，用户不知道哪一方的金额或分类消失了。
- 收据图片和记账记录分离，备份、迁移和恢复容易遗漏附件。
- 家庭成员共享时缺少清晰的完整历史边界。
- 数据被修改或删除后，用户无法查看谁在何时改了什么。
- 许多工具从账户余额、转账和汇率开始建模，给只想记录收支的用户增加了不必要的输入负担。

## 3. 产品目标

### 3.1 目标

- 支持单设备完全离线地记录收入和支出。
- 支持个人、伴侣和小型家庭在多个设备上共享同一个工作区。
- 支持可选的按自然月设置的开支上限和剩余预算展示。
- 支持类别拆分、商户/对方、备注、标签、支付方式标签和附件。
- 支持客户端主动将密文同步到用户提供的 S3/OSS 兼容对象存储。
- 支持同步历史、显式冲突、修订查看和逻辑删除。
- 支持不依赖原对象存储的加密完整备份恢复，以及明文 CSV/JSON 导出。
- 首先交付 Electron 桌面客户端；浏览器存储先做技术验证，PWA 和移动端后置。

### 3.2 非目标

明确不做：

- 银行流水接口、自动导入、银行对账或支付功能。
- 账户实际余额、资产、负债、净资产或专业会计报表。
- 账户间转账、信用卡还款、调整、退款、冲正、作废等用户可见交易类型。
- 账本级多币种、每笔交易多币种或任何汇率服务。
- 服务端明文账务查询、官方托管账号服务或专用 Relay 服务。
- 应用内 Owner/Editor/Viewer 等成员角色和记录级/字段级权限。
- 要求所有离线副本立即安全擦除的删除承诺。
- P0 的完整 PWA、移动端正式客户端或移动端实时后台同步。
- 自动报税、发票合规、企业多租户和以大模型自动分类为核心的功能。
- 产品命名。

## 4. 用户与使用场景

### 4.1 个人用户

用户在通勤、旅行或无网络环境中记录消费，之后在电脑上查看月度收入、支出和分类统计，并补充收据附件。用户可以不设置月度上限，此时产品就是纯收入/支出记录器。

### 4.2 家庭共享

伴侣或家庭成员使用各自设备记录同一个工作区的家庭收入和支出。共享工作区是完整历史信任边界：拥有对象存储访问权限和主密码的设备可以解密完整历史，不提供隐藏某几笔记录的成员权限。需要私密数据时，应创建另一个工作区。

### 4.3 自主管理存储

用户使用自己管理的 S3/OSS 兼容对象存储，或使用其信任的兼容服务。产品客户端直接访问该对象存储；产品不要求用户部署一个额外的专用同步服务器，也不提供官方托管同步服务。

### 4.4 新设备恢复

用户在新电脑上输入对象存储地址/配置、存储凭据和本地主密码，即可下载密文检查点和增量并在本地解密。不需要旧设备、二维码配对、一次性代码或单独恢复密钥。

### 4.5 收据归档

用户离线选择或拍摄收据图片，将其关联到收入或支出。附件在客户端先加密，再单独上传到对象存储；附件上传失败不影响交易本身保存、统计或同步。

## 5. 产品概念与术语

| 术语 | 定义 |
|---|---|
| 工作区 Workspace | 一组分类、收入、支出、标签、附件、历史和同步数据的边界；每个工作区只有一种记账币种和固定精度 |
| 交易 Transaction | 一笔收入或支出，是产品用于统计和预算的基本记录 |
| 分类拆分 Split | 一笔收入或支出内部的多个分类明细；不是独立交易类型 |
| 记录币种 | 工作区创建时选择的唯一币种；工作区创建后不可更改 |
| 草稿 Draft | 只存在于当前设备、尚未保存的编辑内容，不进入统计或同步 |
| 有效版本 | 某笔已保存交易在当前修订历史中用于统计的最新版本 |
| 修订历史 Revision history | 记录已保存交易的字段变化、操作者和时间；不是用户可见的第三种交易类型 |
| 墓碑 Tombstone | 删除记录同步用的无正文标记，只保留身份和版本信息，防止旧设备复活已删除交易 |
| 操作 Operation | 客户端产生的可同步变更及其密文载荷；具体因果字段和编码待技术设计确定 |
| 检查点 Checkpoint | 包含完整工作区状态和审计历史的加密快照，用于新设备恢复和压缩增量 |
| 附件 Blob | 与交易关联的图片或常见文档等独立对象；内容在客户端加密后保存 |
| 冲突 Conflict | 同一交易的并发变更不能安全自动合并时形成的待解决状态 |
| 月度剩余额度/剩余预算 | 某自然月开支上限减去该月最新有效支出的结果，不表示银行、现金、钱包或任何账户余额 |

## 6. 产品原则与行为约束

### 6.1 本地先写

创建、编辑和删除交易先在本地事务中完成，再异步进入同步队列。网络失败不得阻塞录入、查询、本地统计或本地附件索引。

### 6.2 记录事实与派生统计分开

交易、分类、标签、修订、冲突和删除墓碑是需要同步或恢复的工作区数据。月度收入、支出、分类汇总和剩余预算是由本地有效交易重新计算的派生结果，不由远端直接覆盖。

### 6.3 只有两种交易类型

用户可创建的交易只有收入和支出。拆分是交易详情；修改、删除和解决冲突是生命周期操作。退款由用户删除原支出或编辑原支出金额处理：完整退款删除原支出，部分退款编辑原支出金额。这会按原交易日期重新计算历史月份，产品不另建退款或冲正记录。

### 6.4 不维护账户余额

现金、银行卡、钱包等只作为可选的支付方式标签或筛选信息。产品不推导它们的实际余额，不提供账户对账、账户转账或资产负债统计。用户设置的“月度开支上限”只产生剩余预算，不得在界面或文案中称为账户余额。

### 6.5 财务关键字段不使用盲目最后写入

金额、收入/支出类型、记账日期、分类拆分和删除状态的并发修改必须显式解决。互不重叠的非财务字段可以按字段合并；同一标量字段的并发修改仍须提示冲突。时间戳只能用于排序和展示，不能作为财务冲突唯一裁决依据。

### 6.6 端侧明文、远端密文

客户端在解锁后负责解密、校验、应用领域规则和计算报表。对象存储不获得工作区主密码或明文 root key，不承担财务查询和结果裁决。

### 6.7 逻辑删除优先

已保存交易删除后立即从本地摘要和预算中消失，并同步内容为空的墓碑。原内容及修订历史进入延迟清理；MVP 不承诺点击删除后立刻从所有离线设备或对象存储版本中安全擦除。

## 7. 功能需求

### 7.1 创建和解锁工作区

创建工作区：

- 首次启动无需联网即可创建本地工作区。
- 创建时选择唯一的记录币种及固定精度；之后不能在原工作区修改币种或精度。
- 生成随机、高熵的工作区 root key。
- 用户设置本地主密码；客户端用内存困难的密码派生方式在本地派生密钥加密 root key。具体算法和参数属于待研究项。
- 创建完成后立即可以保存第一笔收入或支出。
- 创建流程必须解释：没有任何已解锁客户端时忘记主密码将永久无法恢复密文，并提示用户创建加密完整备份。

解锁和恢复工作区：

- 用户输入对象存储地址/配置、有效存储凭据和本地主密码。
- 客户端下载密文检查点、必要增量、墓碑和附件索引，验证完整性后在端侧解密。
- 恢复不依赖旧设备、二维码、配对代码或单独恢复密钥。
- 本地恢复期间用户应能查看进度；恢复失败必须说明是凭据、密码、完整性或网络问题。
- 对象存储不可访问时，已经解锁的本地副本仍可继续使用。

密码边界：

- 主密码不上传。
- 修改主密码只重新包裹稳定的 root key，不要求重新加密完整工作区。
- 忘记主密码且没有已解锁客户端时，不提供服务端重置或恢复后门；密文永久不可恢复。
- 若仍有已解锁客户端，可由该客户端完成重新包裹 root key 的改密流程。

### 7.2 分类、标签和支付方式

- 支持收入分类和支出分类。
- 支持按需创建、重命名、归档分类；已被交易引用的分类不能直接破坏历史记录。
- 支持标签、商户/对方、备注和可选支付方式标签。
- 支付方式可包括现金、银行卡、电子钱包等，但仅用于描述、筛选和统计，不生成余额。
- 分类和标签的创建、编辑、归档支持离线操作并参与同步。
- 分类或标签变更不能静默改写既有修订历史；历史版本仍可解释其当时的字段值。

### 7.3 交易录入和编辑

用户可见的交易类型只有：

1. 支出
2. 收入

每笔交易支持：

- 金额：以工作区币种的最小单位保存为有符号整数；收入为正，支出为负。界面可以采用更易输入的正数展示，但保存时必须归一化符号。
- 用户明确选择的本地日历日期；该日期决定所属月份。
- 可选的发生时间和原始时区，用于展示和排序。
- 一个分类，或多个分类拆分明细。
- 可选商户/对方、备注、标签、支付方式标签和附件。

分类拆分约束：

- 所有拆分必须使用与交易相同的收入/支出符号。
- 拆分金额之和必须精确等于交易金额，不允许舍入差额或浮点误差。
- 拆分不是独立交易，报表不得重复计算交易总额。

生命周期：

- 草稿仅保存在当前设备，不进入统计、预算或同步。
- 用户点击保存后，交易立即成为有效记录并进入统计、预算和同步队列。
- 不提供“已确认/未确认”字段，因为产品不连接银行，也没有外部对账权威。
- 已保存记录可以编辑或删除；统计始终使用最新有效版本。
- 编辑或删除必须在本地立即生效，并在网络恢复后同步。
- 删除已保存记录生成墓碑；墓碑不能包含被删除交易的正文或旧金额。

产品不提供独立的转账、退款、调整、冲正、作废或还款录入类型。用户需要完整退款时删除原支出，需要部分退款时编辑原支出金额。

### 7.4 月度开支上限和统计

工作区可以为每个自然月配置一个可选的开支上限。

- 未设置上限时，产品就是纯收入/支出记录器，只显示收入、支出、净流量和分类统计。
- 设置上限后，显示“剩余预算”或“剩余月度额度”：

  ```text
  本月剩余预算 = 本月开支上限 - 本月最新有效支出总额
  ```

- 收入不增加开支上限。
- 月份彼此独立，未用额度和超支金额不结转到下月。
- 新月份默认沿用上月的开支上限；用户可以修改或关闭本月设置，不能改写历史月份的上限快照。
- 删除或编辑支出会按该支出的记账日期重算所属月份；不会生成当前月份的退款记录。
- 超支时允许剩余预算显示为负数，并明确标识超支。
- 所有界面文案必须避免把月度剩余预算称为银行、现金、钱包或账户余额。

MVP 报表支持：

- 按日期范围查看收入和支出。
- 按分类、标签、商户/对方和支付方式标签筛选。
- 搜索备注、商户/对方和标签。
- 查看月度收入、支出、净流量和分类汇总。
- 查看已设置月份的开支上限、已用支出和剩余预算。
- 查看交易修订历史、未解决冲突、待同步变更和失败原因。

报表应标识本地未同步、含未解决冲突或附件未完成同步等状态，避免用户把不完整的同步状态误认为全局最终结果。

### 7.5 修订历史和删除

- 每笔未删除的已保存交易保留可展开的字段级修改历史，至少显示修改设备/操作者、修改时间和变更字段。
- 统计和预算只采用最新有效版本，不重复计算旧版本。
- 用户界面默认突出当前版本，历史详情按需展开，避免增加日常录入负担。
- 删除立即影响本地统计，并同步内容为空的墓碑，防止旧客户端将交易重新上传。
- 被删除交易的正文和修订历史进入延迟加密清理；墓碑可以长期保留身份和版本信息。
- MVP 不提供立即安全擦除。对象存储的版本保留、生命周期和离线副本可能延长密文存在时间，产品不得声称删除后立刻不可恢复。

### 7.6 冲突处理

以下字段的并发修改必须显式进入冲突处理：

- 收入/支出类型
- 金额
- 记账日期
- 分类拆分
- 删除状态

以下变更可以在不重叠时自动合并：

- 备注、商户/对方、支付方式等互不重叠的非财务标量字段
- 标签集合和附件集合按元素合并

同一非财务标量字段被并发修改时也必须提示冲突，不能使用最后写入覆盖。

冲突期间：

- 该交易的收入/支出贡献采用最后一个无冲突版本；候选版本不重复计入统计或剩余预算。
- 其他没有冲突的交易继续正常参与统计。
- 报表和交易详情明确标记含未解决冲突。
- 冲突详情展示本地版本、远端版本、修改设备/时间、字段差异及各候选版本对支出和剩余预算的影响。

解决方式：

- 选择一个候选版本。
- 手工合并非财务字段并创建新版本。
- 将候选内容保留为两笔独立交易。

任何解决动作都必须产生新的解决操作，不能直接删除另一候选版本的历史。

### 7.7 直接加密同步

MVP 的同步目标是用户提供的 S3/OSS 兼容对象存储，不部署专用 Relay，不运营官方托管同步账号，也不设置服务端账务数据库。

- 客户端通过对象存储客户端适配器执行上传、下载、检查点恢复和附件操作。
- 同步核心依赖供应商无关的端侧对象存储接口，不把某家厂商 SDK 泄漏到交易和同步领域层。
- MVP 只实现经过测试的 S3 兼容适配器；其他对象存储适配器属于后续工作。
- 对象存储只接收密文，以及对象路由、版本、大小、完整性等最少必要元数据。
- 客户端加密并验证操作载荷、检查点和附件后再上传。
- 拥有对象存储访问权限、完整密文和 root key 的客户端可以在本地重建工作区明文。
- 对象存储故障只影响同步，不影响本地记录和查询。
- 上传必须可重试、可恢复；同一个操作重复发送不能生成重复交易。
- 同步界面显示最后一次成功同步时间、待上传数量、待下载数量和可采取行动的失败原因。

移动端在后续支持时的承诺：

- 用户手动同步、应用启动和恢复前台时，立即发起同步尝试。
- 进入后台后的执行受 Android/iOS 系统限制，只作为机会能力，不保证固定间隔、实时到达或后台必然执行。
- 同步失败不影响本地记账，下一次前台或手动同步继续尝试。

### 7.8 加密和共享信任边界

产品采用端侧可解密、服务端只保存密文的模型：

- 每个工作区生成独立、随机、高熵的 root key。
- 主密码只在本地用于派生加密 root key 的密钥；密码派生方式必须是内存困难方案，具体算法和参数待技术设计确定。
- 远端保存被包裹的 root-key envelope、KDF 所需盐和参数、密文检查点、密文操作、密文附件及必要完整性元数据；不保存主密码或明文 root key。
- 从 root key 派生用途分离的数据密钥，至少区分工作区数据和附件。
- 修改主密码重新包裹稳定的 root key，不重新加密整个工作区。
- 创建工作区、修改主密码和生成备份时提示用户保存加密完整备份。
- 同一工作区内默认共享完整历史。获得对象存储凭据和主密码即进入完整共享信任域；MVP 不做应用内成员角色或记录级权限。
- 若需要阻止仍持有旧 root key 的设备解密未来数据，后续必须进行存储凭据轮换和加密密钥轮换；具体流程是待研究项。
- 已经下载到设备的明文副本无法由对象存储远程擦除。

### 7.9 检查点、增量和保留

对象存储必须始终保留足够的加密状态，使新设备能够只凭对象存储配置、凭据和主密码恢复。

- 客户端定期生成包含完整工作区状态和审计历史的加密检查点，并在其后保存加密增量操作。
- 默认保留最新三个有效检查点，以及最旧保留检查点之后的全部增量。
- 已被保留检查点覆盖的增量默认再保留 30 天后才允许清理。
- 检查点数量和增量宽限期可配置。
- 新设备优先尝试最新检查点；完整性验证失败时可回退到较旧的保留检查点。
- 交易修订、未解决冲突和已解决冲突属于业务数据，必须包含在检查点中；只能压缩已由检查点覆盖的冗余传输操作。
- 附件按内容寻址并始终端侧加密。只要仍被任一保留检查点引用，就不能回收；无引用附件遵循检查点和宽限期规则。

具体对象命名、manifest 格式、操作编码、因果依赖和压缩执行时机不在本 PRD 中定案。

### 7.10 附件

- 支持图片和常见文档格式。
- 文件在客户端加密后上传，服务端/对象存储不能读取正文。
- 使用内容哈希检测重复；大文件支持分块上传和断点续传。
- 附件元数据与交易操作可以分开同步，但交易保存不能等待附件上传完成。
- 界面显示附件待上传、上传中、已完成和失败状态。
- 权威加密完整备份必须包含工作区中的全部附件。
- 可读导出在格式允许时可由用户选择是否包含附件。
- 缩略图、按需原图下载和具体压缩策略待实现阶段验证，不影响附件必须端侧加密的产品约束。

### 7.11 备份、恢复和导出

权威的可携带完整备份是版本化的加密归档包，必须包含：

- 被包裹的 root-key envelope。
- 完整加密检查点。
- 恢复所需的增量操作。
- 删除墓碑。
- 工作区中的全部附件及其完整性清单。
- 可验证归档完整性的 manifest。

加密归档必须能在不连接原对象存储的情况下恢复工作区。

可读导出：

- CSV 用于表格和人工处理。
- 带版本标识的 JSON 用于迁移和第三方工具。
- CSV 至少包含可用于人工处理的最新有效收入/支出及其分类拆分；版本化 JSON 包含工作区结构、交易、修订历史、冲突状态和附件索引。
- 导出默认以明文处理，导出流程必须明确提示隐私风险。
- 在格式允许时由用户选择是否包含附件。
- CSV/JSON 不承担加密备份职责。
- 本地 SQLite 只是客户端内部实现，不是稳定的公开备份或交换格式。

### 7.12 平台和本地持久化

- Electron 桌面端是第一个正式的 P0 客户端，复用平台无关的 Web UI 和领域逻辑。
- Electron 使用原生 SQLite 保存本地工作区；SQLite schema 和文件不是公开兼容格式。
- P0 只包含浏览器 SQLite WASM/OPFS 的技术验证，不承诺完整 PWA。
- 本地持久化必须支持离线重启后恢复收入、支出、分类、历史、冲突、同步队列和附件索引。
- 领域逻辑、同步逻辑和数据迁移应保持平台无关，以便后续评估 PWA 和移动端；具体浏览器适配器和移动技术路线属于延期决策。

## 8. 数据与同步产品约束

### 8.1 金额、币种和日期

- 一个工作区只有一种记录币种和固定精度，创建后不可更改；需要另一种币种时创建新工作区。
- 金额使用该币种最小单位的有符号整数，不使用二进制浮点参与财务计算。
- 外币消费由用户自行换算后录入。原始外币金额可以写入备注，但产品不解析、存储、抓取或重算汇率。
- 每笔交易有用户选择的本地日历日期；月度统计和预算只按该日期归属月份，不因查看设备时区变化。
- 发生时间/时区是可选展示信息。
- 创建、修改和同步时间使用 UTC，仅用于审计和同步，不决定交易所属日期或月份。

### 8.2 派生结果

- 最新有效收入和支出是统计事实。
- 分类汇总、月度收入、月度支出、净流量和剩余预算全部由本地有效版本重算。
- 未解决冲突的交易使用最后无冲突版本贡献；报表必须显示冲突状态。
- 不同步、也不维护现金、银行卡、钱包等账户余额。

### 8.3 客户端同步状态

客户端至少能区分：

```text
本地变更：本地已保存 → 待上传 → 已上传
                         └→ 失败待重试
远端变更：待下载 → 已下载并应用
                    ├→ 冲突
                    └→ 失败待重试
```

这只是用户可理解的状态展示；操作的具体依赖、游标和因果协议待技术设计确定。

## 9. MVP 范围

MVP 覆盖本地记录器和直接加密同步两个交付部分：

- 创建、解锁和恢复单币种工作区。
- 离线收入/支出录入、编辑、删除和分类拆分。
- 分类、标签、商户/对方、备注、支付方式标签。
- 可选的每月开支上限、剩余预算和基础收入/支出报表。
- 草稿、有效版本、修订历史和删除墓碑。
- 字段级并发冲突提示、预览和解决。
- Electron 桌面端和原生 SQLite 本地持久化。
- 浏览器 SQLite WASM/OPFS 技术验证，不承诺完整 PWA。
- 直接连接用户提供的 S3/OSS 兼容对象存储。
- 工作区数据和附件端侧加密，root-key envelope 和主密码边界明确。
- 加密检查点、增量、默认三个检查点和 30 天增量宽限。
- 加密完整备份，以及 CSV 与版本化 JSON 明文导出。
- 基础附件关联、加密、队列和断点上传。

## 10. 实施阶段

### 阶段 0：边界与技术验证

- 验证简化的收入/支出领域模型、分类拆分、月度预算和修订历史。
- 建立 Electron 本地 SQLite 的最小保存、迁移和离线重启验证。
- 做浏览器 SQLite WASM/OPFS 技术 Spike，记录性能、锁、持久化和恢复限制，不在 Spike 前承诺 PWA。
- 验证本地加密/解密边界、密码改包裹和无密码恢复失败路径；具体密码套件与参数仍待安全设计。
- 验证两个客户端之间的新增、编辑、删除、重复上传和冲突展示。
- 验证至少一个受支持的 S3 兼容对象存储适配器；不扩展为通用供应商承诺。

阶段 0 的退出条件是：可以证明本地交易模型、派生统计和跨设备变更模型能够解耦；如果做不到，应先调整领域边界，不扩展 UI。

### 阶段 1：本地记录器

- 完成工作区初始化、唯一币种/精度、分类、标签和支付方式标签。
- 完成收入/支出录入、拆分、编辑、删除和月度开支上限。
- 完成最新有效版本统计、修订历史、墓碑和冲突前基线计算。
- 完成 CSV/JSON 明文导出；定义加密完整备份的版本化格式和恢复测试向量。
- 建立金额整数、日期归属、拆分总和和预算计算的单元测试与性质测试。

### 阶段 2：直接加密同步

- 完成对象存储凭据/配置、主密码解锁和 root-key envelope。
- 完成端侧加密检查点、增量操作、上传/下载、重试、幂等和恢复。
- 完成新设备只凭对象存储配置/凭据和主密码恢复。
- 完成删除墓碑、默认检查点/宽限清理和完整性失败回退。
- 完成冲突收件箱、候选影响预览和解决操作。
- 完成基础附件关联、内容寻址、端侧加密、分块队列和断点上传。
- 完成包含工作区全部附件的加密完整备份，并验证其在无原对象存储时的恢复。
- 用 2–5 个客户端进行离线交错、断网、重复上传和并发编辑测试。

### 阶段 3：附件和后续客户端

- 完善附件引用保留、无引用回收、缩略图和按需下载体验。
- 根据 OPFS Spike 结果决定是否支持 PWA。
- 再评估移动端正式客户端及其前台/后台同步机会能力。
- 其他对象存储适配器、密钥轮换和更复杂的访问控制另行立项。

## 11. 验收标准

### 本地优先和产品语义

- 关闭网络后仍可创建、编辑、删除草稿和已保存收入/支出，并查看报表和已配置月份的剩余预算。
- 应用重启后，交易、修订、冲突、同步队列和附件索引完整存在。
- 对象存储不可用不会阻塞本地保存或本地查询。
- 用户可见交易类型只有收入和支出；没有账户余额、转账、退款、调整、冲正、作废或确认状态入口。
- 工作区只能使用创建时选择的一种币种；外币不触发汇率服务。
- 金额整数计算无浮点舍入误差；拆分之和始终精确等于交易金额。
- 月度归属只由用户选择的本地日期决定；预算不结转，收入不增加开支上限。

### 同步和恢复

- 两台或更多设备离线各自新增收入/支出，恢复后所有有效记录最终都存在且不重复。
- 同一操作重复上传不会生成重复交易。
- 对象存储中可验证不存在账目、附件和主密码明文。
- 新设备只凭对象存储配置/凭据和主密码即可从保留密文状态恢复，无需旧设备。
- 最新检查点完整性失败时可以回退到较旧的有效检查点。
- 删除后的旧设备不会通过重放旧变更复活交易；墓碑能被同步和恢复。
- 已由检查点覆盖的操作按保留规则进入延迟清理；被保留检查点引用的附件不会被回收。
- 手动同步、启动和前台恢复会发起同步尝试；未来移动端后台不作固定时限承诺。

### 冲突、历史和统计

- 同一交易的金额、类型、日期、分类拆分或删除状态并发修改不会使用盲目最后写入覆盖。
- 不重叠的备注、商户/对方、支付方式等字段可以自动合并；同一标量字段冲突会提示用户。
- 冲突期间交易使用最后无冲突版本贡献，候选版本不重复计入；其他交易正常统计。
- 用户可以查看候选差异、预算影响并选择候选、手工合并或保留两笔独立交易。
- 未删除交易的编辑和冲突解决能在历史中显示操作者/设备、时间和字段变化；删除立即生效，墓碑只保留身份和版本元数据，被删除正文及其修订历史按延迟清理规则处理。
- 统计只采用最新有效版本，删除立即从摘要和预算中消失。

### 加密、密钥和数据边界

- 主密码、明文 root key 和明文业务数据不上传对象存储。
- 修改主密码不要求重新加密完整工作区。
- 忘记主密码且没有已解锁客户端时，服务端不能恢复数据，也没有重置后门。
- 创建和改密流程提示数据丢失边界，并引导创建加密完整备份。
- 同一工作区共享完整历史；需要私密数据时可以通过新工作区隔离。
- 已下载到其他设备的明文副本不被产品宣称可远程擦除。

### 备份、导出和体验

- 加密完整备份包含工作区中的全部附件，可以脱离原对象存储恢复工作区并校验完整性。
- CSV 和版本化 JSON 可被用户主动导出，并明确提示其为明文；可读导出的附件包含与否遵循用户选择。
- SQLite 文件不被当作公开兼容备份格式。
- 同步界面显示最后成功时间、队列数量和失败原因。

## 12. 主要风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 简化为记录器后，未来有人需要真实账户余额或资产统计 | 中 | 在产品边界中明确不提供账户余额；若需求稳定出现，另立领域设计，不把账户语义偷偷塞回 MVP |
| 浏览器 SQLite/OPFS 行为和性能不一致 | 高 | P0 只做技术 Spike；Electron 首发，领域层不绑定浏览器实现 |
| 并发金额、日期或拆分修改处理不正确 | 高 | 关键字段显式冲突；建立交错离线、性质和恢复测试；不使用盲目 LWW |
| 端到端加密导致主密码丢失 | 高 | 创建、改密和备份流程反复提示；无已解锁客户端时不提供恢复后门 |
| 对象存储供应商的 S3 兼容性存在差异 | 中高 | 通过供应商无关端口隔离；MVP 只承诺经过测试的适配器，建立兼容性测试后再扩展 |
| 检查点、增量和附件造成存储增长 | 中 | 默认三个检查点、30 天宽限、内容寻址、引用保护和可配置保留策略 |
| 凭据轮换和旧设备撤销边界粗粒度 | 中高 | 明确共享完整信任域；后续研究存储凭据/加密密钥轮换，现阶段不声称记录级撤销 |
| 移动端后台执行受系统或厂商限制 | 中 | 只保证手动、启动和前台恢复时发起尝试；后台为机会能力并显示最后同步状态 |
| 自定义许可证缺少 OSI/SPDX 识别 | 中 | 保留仓库现有 `license` 文本，接受闭源衍生和收费托管，同时在贡献与发布文档中说明许可边界 |
| Actual Budget 的模型或许可证不适合直接采用 | 中 | 只作研究参考；不 fork、不依赖 `loot-core`；任何未来代码移植单独做适配和许可审查 |

## 13. 待研究与延期事项

下列内容是后续技术设计或验证的输入，不是本 PRD 已选定的实现方案：

1. 具体密码套件、认证加密方式、随机数要求、内存困难 KDF 的算法和参数，以及经审计密码库和独立安全评审方案。
2. root-key envelope、检查点、增量操作、墓碑和附件 manifest 的不可变对象布局、按设备操作流、发现/发布流程、条件写入和损坏回退机制。
3. 操作载荷编码、游标、因果修订图、并发检测和压缩协议。
4. S3/OSS 兼容供应商矩阵、认证方式、错误语义、分块上传差异和凭据配置体验；未经兼容性测试不得宣称支持。
5. 主密码修改之外的 root key 轮换、设备/凭据撤销和旧副本处理机制；在原型验证前不得声称可阻止旧密钥持有者解密未来数据。
6. 浏览器 SQLite WASM/OPFS 的锁、持久化、容量、恢复和多标签行为；Spike 完成后再决定 PWA 支持边界。
7. 检查点生成触发条件、压缩调度、版本保留配置的具体界面和实现策略。
8. 移动端正式技术路线、系统后台任务接入和通知策略。
9. 除已测试 S3 兼容适配器之外的对象存储适配器。
10. 未来若需要细粒度成员权限或隐私分区时的密钥分层设计。
11. 版本化 JSON 导出和加密完整备份的 schema、向前迁移规则与恢复测试向量。
12. 未来若重新引入专用 Relay，其数据库必须隔离在通用持久化端口之后；MVP 不设计 Relay 数据库抽象，也不承诺 PostgreSQL/MySQL 兼容性。

## 14. 原 PRD 八项待决策的关闭情况

| 原第 14 节问题 | 当前结论 |
|---|---|
| Actual Budget fork、抽取 `loot-core` 还是参考 | 仅作 schema、交易概念、迁移和测试参考；不 fork，不建立代码依赖 |
| Web SQLite WASM/OPFS 或兼容适配器 | P0 只做 SQLite WASM/OPFS 技术验证；Electron 原生 SQLite 首发，PWA 决定延期 |
| Relay 元数据数据库使用 SQLite 还是 PostgreSQL | 已被新架构取代：不部署专用 Relay、不提供 Relay 数据库；客户端直连对象存储 |
| 永久保留操作日志还是允许压缩 | 默认保留三个有效加密检查点；保留其后增量；已覆盖增量默认宽限 30 天后清理，具体机制可配置 |
| 账本级多币种及汇率 | 每工作区单一币种和固定精度；外币由用户手工换算；不提供汇率模型 |
| 附件是否端到端加密、是否第三方对象存储 | 附件始终端侧加密，并存放于用户提供的同一 S3/OSS 兼容对象存储；不提供服务端明文附件 |
| Android/iOS 后台主动同步保证 | 手动、启动、前台恢复保证立即发起尝试；后台仅机会能力，无实时或固定间隔承诺 |
| 家庭成员是否共享完整历史 | 是；对象存储凭据和主密码定义完整共享信任域，MVP 不提供 Owner/Editor/Viewer 或记录级权限 |

## 15. 许可与上游参考

- 保留仓库现有自定义 `license` 文本，不在本 PRD 中替换为其他许可证。
- 接受基于本项目制作闭源衍生版本和收费托管服务。
- 同时接受该自定义许可证缺少标准 OSI/SPDX 识别，可能给贡献者、再发布者和自动化合规工具带来解释不确定性。
- Actual Budget 及其 `loot-core` 只用于理解账务模型、迁移、测试和产品取舍；本项目不 fork，也不把 `loot-core` 作为依赖。
- 若未来需要移植某个独立模块，必须先做适配性和许可证/上游声明审查，并保留适用的上游声明。

## 16. 产品边界总结

> 账目在端侧以明文工作；对象存储只保存密文；产品记录收入和支出，并可选地计算月度剩余预算；同步层负责可靠传播和恢复，不能静默裁决财务冲突。

只要这条边界保持清晰，后续替换桌面、浏览器或移动端客户端，增加新的对象存储适配器，或改进检查点实现，都不需要把产品重新变成账户余额系统，也不需要让服务端看到账本明文。
