# 用户录屏反馈证据

日期：2026-09-15

## Source

- `/tmp/tmp/simplescreenrecorder-2026-09-15_09.09.40.mkv`
- 约 29.05 秒，2878×1670，H.264，60 fps。
- 录屏使用 Web 账单页，地址栏可见 `/ledger?month=...`；仅作本地人工视觉证据，不提交视频或录屏中的账单数据。

## Observations

- 月份控件连续切换时，页面会短暂出现空白或独立的月度 loading 卡，再回到完整账单页；这与当前 shell 在目标 snapshot 未就绪时切换到独立 `LedgerMonthLoading` 的实现相符。
- Web 页面当前把“添加交易”按钮放在标题左侧，月份控件右侧下方留下空白；用户建议把交易按钮放到月份控件正下方。
- 在切换到历史月份后打开新增交易弹层，日期字段沿用所选月份并显示该月 1 日（录屏中可见 `2026/01/01`）；用户要求新增交易默认定位到今天。

## Interpretation boundary

录屏可以确认可见现象和布局意图，但不能单独证明异步竞态、缓存命中或 API 错误原因；实现仍以 renderer 源码、Query 状态和 Playwright 延迟读取回归为准。
