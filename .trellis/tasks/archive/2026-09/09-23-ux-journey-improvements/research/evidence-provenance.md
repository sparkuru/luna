# 证据来源与规划校验

2026-09-23 将上一轮用户授权的 Web 体验审计从 `/tmp/luna-ux-audit-20260922` 保存到本目录。共复制 24 个文件，包括原报告、报告直接引用的关键截图、JSON 和复现脚本。原报告提到 64 张审计截图，此目录只保存报告直接引用的关键截图，不声称复制了全部原始工件。

审计数据均为独立浏览器中的合成账本。本地 API/数据库及对象存储夹具的覆盖不能代替真实远端部署。原报告中的时间、验证结果、完成检查和工作区快照都是当时证据，不代表本次规划已经实施。

复现脚本包含当时机器的绝对 Node 模块路径及 4187/4188 预览地址。当前仓库中的截图、行为结果和测试 JSON 可独立阅读；重新执行脚本前需选择当前预览地址/模块路径。未来正式回归应使用项目 Playwright 配置，不能依赖原临时服务器仍运行。

## 本轮规划校验

- 六个任务分别运行 `python3 .trellis/scripts/task.py validate <task-dir>`，全部通过；五个任务各有 9 条 implement/check 上下文，删除恢复设计各有 11 条。
- 六个任务状态均为 planning；父任务 children 与五个子任务 parent 双向一致；当前未激活任务。
- 18 份 prd/design/implement 文档无 TBD/_example 占位；所有 Markdown 规划链接和原报告直接引用工件存在。
- F01–F09 全部映射到子任务，原始严重性、来源及安全边界保留。PRD 按目标、证据、需求、验收、范围和风险收敛；技术方案及执行步骤分别放在 design/implement。
- `git diff --check` 通过。本轮只新增六个任务目录，未改此前产品源文件或测试；既有未提交变更仍保留。
- 当前没有运行新的产品验证：本轮是文档规划。生产 122/122 等数值来自先前实际审计，后续实施后必须重新运行。

下一步选择并批准相应子任务的规划后再启动；建议先 A，其次 B/C/D。E 为独立设计交付，不自动授权实际撤销实现。
